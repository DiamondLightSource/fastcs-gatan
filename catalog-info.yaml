apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: fastcs-gatan
  title: fastcs-gatan
  description: Gatan K3 detector driver in FastCS, acting as a client to the SerialEMCCD plugin on the DigitalMicrograph computer
spec:
  type: service
  lifecycle: experimental
  owner: user:TODO # set to the actual component owner before this is registered
